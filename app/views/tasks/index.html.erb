<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, show_path(task) %><%= link_to "Delete",
      delete_path(task),
      data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
    </li>
  <% end %>
  <li><%= link_to 'Add a new task',  addNew_path %></li>
</ul>
